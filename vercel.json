{
  "version": 2,
  "builds": [
    {
      "src": "src/main.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["src/**/*.js", "src/**/*.json"]
      }
    }
  ],

  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/main.ts"
    }
  ]
}